# ─────────────────────────────────────────────────────────
# PDF Compressor Microservice (Ghostscript)
# ─────────────────────────────────────────────────────────
# URL of the deployed pdf-compressor-service Docker container.
# Deploy to Render: https://dashboard.render.com
# Then set the URL Render gives you here (no trailing slash).
#
# For local development only (requires Ghostscript installed on your machine):
# VITE_COMPRESSOR_URL=http://localhost:3001
VITE_COMPRESSOR_URL=https://your-pdf-compressor-service.onrender.com
